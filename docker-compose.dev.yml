services:
  application:
    build: ./app
    container_name: application-dev
    environment:
      - PORT=3000
      - BG_COLOR=green
    networks:
      - testnet

networks:
  testnet:
    external: true
