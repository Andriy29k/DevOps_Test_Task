services:
  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./tls:/tls
    ports:
      - "80:80"
      - "443:443"
    networks:
      - testnet

networks:
  testnet:
    driver: bridge